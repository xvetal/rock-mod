import { RockMod } from "./RockMod";

const rockMod = new RockMod({
  multiplayer: "RageMP",
});

rockMod.init();

// const player = rockMod.players.getByID(0);
// const vehicle = rockMod.vehicles.getByID(0);

// player.model = "123";
